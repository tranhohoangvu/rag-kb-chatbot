# RAG Knowledge Base Chatbot

Stack: FastAPI + Postgres(pgvector) + React(Vite) + Tailwind + Docker Compose

## Quickstart
1) Copy `.env.example` -> `.env` and fill values
2) `docker compose up -d db`
3) Run backend + frontend (see docs later)
